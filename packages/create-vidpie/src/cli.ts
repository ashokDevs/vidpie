#!/usr/bin/env node

/**
 * Scaffold entry point, not implemented yet. It exits non-zero so that an
 * accidental early publish fails loudly rather than appearing to work.
 */
process.stderr.write('create-vidpie is not implemented yet.\n');
process.exit(1);
